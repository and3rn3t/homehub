name: Dependency Review

on:
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  dependency-review:
    uses: and3rn3t/.github/.github/workflows/reusable-dependency-review.yml@main
