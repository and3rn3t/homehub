name: CodeQL Security Analysis

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: "0 2 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    uses: and3rn3t/.github/.github/workflows/reusable-codeql.yml@main
    with:
      languages: '["javascript", "typescript"]'
