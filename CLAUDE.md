# CLAUDE.md — HomeHub

**Read `AGENTS.md` first — it is the single source of truth for stack, commands, and conventions.** This file only holds Claude-specific additions.

## Claude-specific notes

- Run `npm run validate` before declaring a change done.
- Prefer the Cloudflare MCP connector (Cowork sessions) for inspecting Pages/Workers over guessing from config.
